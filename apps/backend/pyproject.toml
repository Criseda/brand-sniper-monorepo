[project]
name = "backend"
version = "0.1.0"
description = "Core Compute Engine REST API & FastMCP Agent Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.28.0",
    "shared-utils",
]

[tool.uv.sources]
shared-utils = { workspace = true }